[project]
name = "clio-backend"
version = "0.1.0"
description = "CLIO v1 backend — Historical Decision Intelligence"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]

[tool.setuptools.packages.find]
include = ["app*"]
